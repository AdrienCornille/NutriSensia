import { MFATest } from '@/components/auth/MFATest';

/**
 * Page de test pour l'authentification à deux facteurs (2FA)
 * Permet de tester toutes les fonctionnalités 2FA de manière interactive
 */
export default function MFATestPage() {
  return <MFATest />;
}
