'use client';

import { HeroSection } from '@/components/landing/HeroSection';

/**
 * Exemple d'utilisation de la Hero Section
 *
 * Ce composant démontre l'utilisation de la Hero Section
 * avec toutes les animations et interactions.
 */
export function HeroExample() {
  return (
    <div className='min-h-screen'>
      <HeroSection />
    </div>
  );
}
